change let expression = line.substring(line.indexOf("(") + 1, line.indexOf(")")) to use lastIndexOf

FINISH EXAM MODE!!!!!!!!!!!!!
2.9 
User will enter exam mode
User will click the question button
User will click the answer button
The pConsole will display a random exam question from the database.

The pConsole will then display the answer for the currently displayed exam question.



